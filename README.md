This is a Menulet built from rumps which is used to display hourly and ten day weather. It will work as a menulet in Mac OS.

Required: python3.5
virtualenv --python=/Users/$USER/.pyenv/versions/3.5.0/bin/python3.5 venv

Required: Mac OS
